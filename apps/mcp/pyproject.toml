[project]
name = "openagentsearch-mcp"
version = "0.1.0"
description = "OpenAgentSearch MCP service"
requires-python = ">=3.11"
dependencies = [
    "fastmcp==3.1.0",
    "httpx==0.28.1",
]

[tool.uv]
package = false
